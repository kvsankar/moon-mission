#!/usr/bin/env sh
set -eu

echo "[pre-commit] syncing mission config.json from config.json5..."
npm run -s configs:compile

# Stage any generated config.json updates for this commit.
git add assets/*/data/config.json 2>/dev/null || true

echo "[pre-commit] checking staged JavaScript structure..."
npm run -s structure:check:staged

echo "[pre-commit] done"
